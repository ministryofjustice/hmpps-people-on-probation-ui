# The Fingerprint of false positive alerts can be added here to be excluded from future scans.

# False positives: high-entropy regex literals (document name validation pattern), not secrets.
assets/js/documents.ts:token:50
server/utils/documentTypes.ts:token:22
